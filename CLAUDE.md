# CLAUDE.md

@AGENTS.md

The above is the canonical, tool-agnostic reference — also read by Cursor and any other agent.

## Skills

`.cursor/skills` is a directory symlink to `.claude/skills` — same files, no copies. Claude Code
auto-discovers and invokes them by task the same way Cursor does.
